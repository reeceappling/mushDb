apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "mushDb.applicationName" . }}
  labels:
    mushroomNode: main
spec:
  replicas: 1
  selector:
    matchLabels:
      mushroomNode: main
  template:
    metadata:
      name: {{ include "mushDb.applicationName" . }}-pod
      labels:
        mushroomNode: main
    spec:
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: api
          image: mush-api:latest
          imagePullPolicy: Never
          ports:
            - name: http-api
              containerPort: 80
          volumeMounts:
            - name: mushroom-images-storage
              # TODO: next line must match go and tsx
              mountPath: {{ quote .Values.picsPathAbsolute }}
          env:
            - name: "PICS_PATH"
              # TODO: next line must match go and tsx and above
              value: {{ quote .Values.picsPathAbsolute }}
            - name: "API_PORT"
              value: {{ .Values.localPort | default 80 | quote }}
            - name: "MONGO_INITDB_USERNAME"
              value: {{ .Values.dbRoot.username }}
            - name: "MONGO_INITDB_PASSWORD"
              value: {{ .Values.dbRoot.password }}
            - name: "MONGO_INITDB_DATABASE"
              value: {{ .Values.dbName }}
            - name: "RFID_SECRET"
              value: {{ quote .Values.rfidSecret }}
            - name: "DB_HOST_PORT"
              value: {{ .Values.dbPort | default "27017" | quote }}
            - name: "DB_HOST_NAME"
              value: {{ quote .Values.dbhostname }}
            - name: "WEB_HOST_INTERNAL"
              value: {{ .Values.webserverInternalHost | default "localhost" | quote }}
            - name: "WEB_HOST_INTERNAL_PORT"
              value: {{ .Values.webserverInternalPort | default "3000" | quote }}
            - name: "GOOGLE_CLIENT_ID"
              value: {{ quote .Values.googleClientId }}
            - name: "GOOGLE_CLIENT_SECRET"
              value: {{ quote .Values.googleClientSecret }}
        - name: web
          image: mush-web:latest
          imagePullPolicy: Never
          ports:
            - name: http-web # TODO: the image likely needs env vars?
              containerPort: 3000
        - name: mush-db
          image: {{ include "mushDb.dbImage" . }}
          imagePullPolicy: IfNotPresent
          args: ["--dbpath","/data/db"]
          ports:
            - name: dbport
              containerPort: 27017
          volumeMounts:
            - name: "mushroom-database-storage"
              mountPath: "/data/db"
            - name: "mongodb-scripts"
              mountPath: "/docker-entrypoint-initdb.d"
              readOnly: true
          env:
{{/*            - name: "MONGO_INITDB_ROOT_USERNAME"*/}}
{{/*              value: {{ .Values.dbRoot.username }}*/}}
{{/*            - name: "MONGO_INITDB_ROOT_PASSWORD"*/}}
{{/*              value: {{ .Values.dbRoot.username }}*/}}
{{/*            - name: "MONGO_INITDB_USERNAME"*/}}
{{/*              value: {{ .Values.dbUser.username }}*/}}
{{/*            - name: "MONGO_INITDB_PASSWORD"*/}}
{{/*              value: {{ .Values.dbUser.password }}*/}}
{{/*            - name: "MONGO_INITDB_DATABASE"*/}}
{{/*              value: {{ .Values.dbName }}*/}}
      volumes:
        - name: mongodb-scripts
          configMap:
            name: mongodb-configmap
            items:
              - key: ensure-users.js
                path: ensure-users.js
        - name: mushroom-database-storage
          persistentVolumeClaim:
            claimName: mushroom-database-storage
        - name: mushroom-images-storage
          persistentVolumeClaim:
            claimName: mushroom-images-storage
        - name: test-storage
          persistentVolumeClaim:
            claimName: test-storage
---
apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: mush-host-sc
  annotations:
    storageclass.kubernetes.io/is-default-class: "true"
provisioner: microk8s.io/hostpath
reclaimPolicy: Retain
allowVolumeExpansion: true
volumeBindingMode: WaitForFirstConsumer
---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: "init-pv"
  labels:
    type: "local"
spec:
  storageClassName: mush-host-sc
  capacity:
    storage: "1Gi"
  accessModes:
    - "ReadWriteOnce"
  hostPath:
    path: "/tmp/mush"
    type: "Directory"
---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: "db-pv"
  labels:
    type: "local"
spec:
  storageClassName: mush-host-sc
  capacity:
    storage: "1Gi"
  accessModes:
    - "ReadWriteMany"
  hostPath:
    path: {{ quote .Values.dbPath }}
    type: "DirectoryOrCreate"
---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: "imgs-pv"
  labels:
    type: "local"
spec:
  storageClassName: mush-host-sc
  capacity:
    storage: "1Gi"
  accessModes:
    - "ReadWriteMany"
  hostPath:
    path: {{ quote .Values.picsPathAbsolute }}
    type: "DirectoryOrCreate"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "mushroom-database-init-script-dir"
spec:
  volumeName: "init-pv"
  storageClassName: mush-host-sc
  accessModes:
    - "ReadWriteOnce"
  resources:
    requests:
      storage: "1Gi"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "mushroom-database-storage"
spec:
  volumeName: "db-pv"
  storageClassName: mush-host-sc
  accessModes:
    - "ReadWriteMany"
  resources:
    requests:
      # TODO: reevaluate
      storage: "1Gi"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: "mushroom-images-storage"
spec:
  volumeName: "imgs-pv"
  storageClassName: mush-host-sc
  accessModes:
    - "ReadWriteMany"
  resources:
    requests:
      # TODO: reevaluate
      storage: "1Gi"