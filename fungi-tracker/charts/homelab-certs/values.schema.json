{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "title": "Values",
  "type": "object",
  "required": [
    "email"
  ],
  "properties": {
    "email": {
      "description": "Email to use for ssl certs with the letsEncrypt issuer",
      "type": "string",
      "format": "email"
    }
  }
}