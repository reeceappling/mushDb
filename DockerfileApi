FROM --platform=linux/arm64 golang:latest AS builder

WORKDIR /app
COPY bin/mushApi server

FROM --platform=linux/arm64 alpine:latest

# TODO: workdir ok?
WORKDIR /root/s
COPY --from=builder /app/server .
RUN chmod 777 server
# TODO: ensure next ok
COPY initDB/mongo-init.js docker-entrypoint-initdb.d/mongo-init.js
EXPOSE 443
# TODO: remove 80?
EXPOSE 80
CMD ["./server"]