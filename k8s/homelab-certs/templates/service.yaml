apiVersion: v1
kind: Service
metadata:
  name: {{ include "homelab-certs.fullname" . }}
  labels:
    {{- include "homelab-certs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "homelab-certs.selectorLabels" . | nindent 4 }}
